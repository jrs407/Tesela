spring.application.name=tesela-backend

# Origenes permitidos por CORS (separados por comas). En produccion: URL publica del frontend.
tesela.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}
